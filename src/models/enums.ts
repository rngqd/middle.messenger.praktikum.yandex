export enum RouterPath {
  default = "/",
  login = "/sign-in",
  register = "/sign-up",
  chats = "/messenger",
  profile = "/settings",
  editProfile = "/settings/edit",
  editProfilePassword = "/settings/edit-password",
  clientError = "/404",
  serverError = "/500",
  
}
